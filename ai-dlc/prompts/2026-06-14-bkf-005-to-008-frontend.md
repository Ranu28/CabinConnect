# Prompt Log — BKF-005 through BKF-008: Booking Flow Frontend + Supporting Backend

**Date:** 2026-06-14
**Units:** BKF-005, BKF-006, BKF-007, BKF-008
**Bolt:** BOLT-002
**Output Format:** Full implementation (React/TypeScript pages + supporting C# backend endpoints)

---

## Prompt Quality Gate

| Component | Value |
|---|---|
| **Context** | CabinConnect frontend (React 18 + TypeScript + Vite + react-router-dom v7) and backend (C# .NET 10) — booking flow pages and supporting GET endpoints |
| **Constraints** | CLAUDE.md rules; Supabase auth client for tokens only; React calls .NET API only (no direct Supabase data calls); EC-007 ownership; EC-003 UTC dates; TypeScript strict mode; no `any` |
| **Acceptance Criteria** | ACs from bkf-005 (7), bkf-006 (7), bkf-007 (6), bkf-008 (8) |
| **Output Format** | New React pages/components + new API endpoints + App.tsx routing |

---

## Files Generated

### Backend

| File | Purpose |
|---|---|
| `src/backend/CabinConnect.Domain/Search/ICabinSearchRepository.cs` (updated) | Added `GetCabinByIdAsync` |
| `src/backend/CabinConnect.Domain/Search/CabinSearchService.cs` (updated) | Delegates `GetCabinByIdAsync` |
| `src/backend/CabinConnect.Infrastructure/Search/CabinSearchRepository.cs` (updated) | Implements `GetCabinByIdAsync` (SELECT + seasonal rates JOIN) |
| `src/backend/CabinConnect.Domain/Bookings/GuestBookingItem.cs` | New record for booking list items (with `CabinName`) |
| `src/backend/CabinConnect.Domain/Bookings/IBookingRepository.cs` (updated) | Added `GetGuestBookingsAsync` + `GetGuestBookingByIdAsync` |
| `src/backend/CabinConnect.Domain/Bookings/BookingService.cs` (updated) | Delegation methods |
| `src/backend/CabinConnect.Infrastructure/Bookings/BookingRepository.cs` (updated) | Paginated list + single-booking query (both with guest_id filter) |
| `src/backend/CabinConnect.Api/Controllers/CabinsController.cs` (updated) | `GET /api/cabins/{id}` — AllowAnonymous |
| `src/backend/CabinConnect.Api/Controllers/BookingsController.cs` (updated) | `GET /api/bookings` (paginated) + `GET /api/bookings/{id}` |

### Frontend

| File | Purpose |
|---|---|
| `src/lib/api-client.ts` | `apiFetch()` — adds Bearer token from Supabase session to every request |
| `src/features/auth/use-auth.ts` | `useAuth()` — wraps `supabase.auth.onAuthStateChange` |
| `src/features/auth/require-auth.tsx` | `RequireAuth` — auth guard; redirects to `/login?returnUrl=…` |
| `src/features/auth/login-page.tsx` | Email+password login via Supabase |
| `src/features/cabins/cabin-types.ts` | `CabinDetail` interface |
| `src/features/cabins/cabin-detail-page.tsx` | `/cabins/:id` — fetch cabin, date picker, place hold, navigate to checkout |
| `src/features/booking/booking-types.ts` | `GuestBookingItem`, `HoldCheckoutState`, `BookingConfirmState` |
| `src/features/booking/checkout-page.tsx` | `/checkout?holdId=…` — countdown from `expiresAt`, confirm booking |
| `src/features/booking/booking-confirmed-page.tsx` | `/booking-confirmed?bookingId=…` — read-only success page |
| `src/features/booking/my-bookings-page.tsx` | `/my-bookings` — paginated list, cancel action |
| `src/App.tsx` (updated) | Routes for all new pages |
| `src/features/search/search-results-view.tsx` (updated) | Wires `onSelect` to navigate to `/cabins/:id` with search dates as state |

---

## AC Coverage

### BKF-005 Cabin Detail Page
| AC | Status |
|---|---|
| AC-1 Cabin details displayed | `fetch(/api/cabins/{id})` → render name/desc/image/guests/amenities/location |
| AC-2 Dates pre-populated from search | `location.state.checkIn/checkOut` seeds date inputs |
| AC-3 Total price shown | `nights × baseRate` computed locally; shown when dates valid |
| AC-4 Reserve disabled without valid dates | `disabled={!datesValid}` on button |
| AC-5 Reserve triggers Hold and navigates | `apiFetch(POST /api/holds)` → navigate to `/checkout?holdId=…` |
| AC-6 CABIN_UNAVAILABLE error shown | 409 → inline error message |
| AC-7 Unauthenticated → redirect to login | `useAuth()` check before POST; navigates to `/login?returnUrl=…` |

### BKF-006 Checkout Page
| AC | Status |
|---|---|
| AC-1 Hold details displayed | Reads `HoldCheckoutState` from router state or sessionStorage |
| AC-2 Countdown from server `expiresAt` | `setInterval` recomputes from `new Date(expiresAt) - Date.now()`; refresh-safe |
| AC-3 Confirm → navigate to /booking-confirmed | `apiFetch(POST /api/bookings)` → navigate on 201 |
| AC-4 Countdown zero → button disabled + message | `expired = secondsLeft <= 0`; button `disabled={expired}` |
| AC-5 API HOLD_EXPIRED → expired state | 409 HOLD_EXPIRED → `setSecondsLeft(0)` |
| AC-6 Auth guard | Wrapped in `<RequireAuth>` |
| AC-7 Invalid holdId → redirect to / | `useEffect` → navigate(`/`, replace) when holdState is null |

### BKF-007 Booking Confirmation Page
| AC | Status |
|---|---|
| AC-1 Booking details displayed | Router state or fallback `GET /api/bookings/{id}` |
| AC-2 "Your booking is confirmed!" heading | Rendered unconditionally |
| AC-3 Link to /my-bookings | `<Link to="/my-bookings">View my bookings</Link>` |
| AC-4 Link to / | `<Link to="/">Back to search</Link>` |
| AC-5 Invalid bookingId → redirect to / | API returns error → `navigate('/', replace)` |
| AC-6 Auth guard | Wrapped in `<RequireAuth>` |

### BKF-008 Guest My Bookings
| AC | Status |
|---|---|
| AC-1 Returns all Guest's Bookings | `GET /api/bookings` (JOIN cabins, guest_id filter, ORDER BY check_in DESC) |
| AC-2 Unauthenticated → 401 | `[Authorize]` on endpoint |
| AC-3 Paginated | `page` + `pageSize` query params; COUNT(*) OVER() for total |
| AC-4 Only Guest's own bookings | `WHERE b.guest_id = @GuestId` (EC-007) |
| AC-5 List displays cabin name, dates, price, status | Rendered per item in `MyBookingsPage` |
| AC-6 Cancel action for Confirmed | Button on Confirmed/Pending items → confirm dialog → `POST /api/bookings/{id}/cancel` → optimistic status update |
| AC-7 Empty state with "Search cabins" link | Rendered when `items.length === 0` |
| AC-8 Auth guard | Wrapped in `<RequireAuth>` |

---

## Known Gaps

- **BKF-005 AC-3 price accuracy**: Price is estimated as `nights × baseRate` (ignores seasonal rates). The unit notes "OR computed client-side via the same rate logic" — seasonal rates require calling the server. Flagged as retro watch item; the confirmed price is always server-computed at the time of booking.
- **BKF-008 pagination UI**: The `/my-bookings` page loads the first page only; pagination UI not added (stretch for next iteration).
- **Frontend unit tests**: Not added for the new pages. Existing 24 vitest tests all pass. New page tests require mocking `apiFetch` and Supabase — flagged for the retro.
